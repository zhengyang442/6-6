# 反着来 — 当前配合清单

> 后端/架构 → 全队

---

## 1. 后端状态

| 项目 | 状态 |
|------|------|
| Flask 服务 | ✅ 运行中，`localhost:5000` |
| DeepSeek AI 出题 | ✅ 已接通 |
| 接口数量 | 5 个（health / generate-question / analyze-performance / generate-share-text / create-challenge） |

---

## 2. 现在每个人该做什么

### 前端A ← 你立刻要做的事

**拿文件**：`api-contract.json`（桌面/抖音创变者计划/）

**写代码**：
1. 新建 `index.html`，用 Canvas 渲染游戏界面
2. 复制 `api-client.js`（后端已写好，直接用）进你的项目
3. 调用 `generateQuestion()` 拿题目 → 渲染 → 等用户触控 → 判正误

**你的两个核心函数**（api-client.js 已封装好）：
```javascript
const q = await generateQuestion("easy");
// q.instruction_text → "不要点红色的"
// q.options → [{label:"红",action:"tap_red",color:"#EE4444"}, ...]
// 用户点"蓝" → q.correct_action === "tap_blue" → 正确！

await analyzePerformance(answers); // 游戏结束调这个
```

**第 4 小时必须做到的**：浏览器打开 `index.html`，看到一道题，点对加分点错扣分。

---

### UI·B ← 你立刻要做的事

**输出 `design-tokens.json`**，格式如下：

```json
{
  "colors": {
    "bg": "#1A1A2E",
    "card_bg": "#16213E",
    "correct": "#00FF88",
    "wrong": "#FF4444",
    "text_primary": "#FFFFFF",
    "text_secondary": "#AAAAAA",
    "accent": "#FFD700"
  },
  "fonts": {
    "heading": "sans-serif bold",
    "body": "sans-serif"
  },
  "button": {
    "width": 160,
    "height": 80,
    "corner_radius": 12,
    "font_size": 20
  },
  "layout": {
    "canvas_width": 390,
    "canvas_height": 844,
    "padding": 24
  }
}
```

前端A 会从 `design-tokens.json` 读这些值——他**不硬编码颜色**，全部从你的文件来。

**然后**：出按钮切图（正确/错误反馈的视觉效果），存成 PNG，命名规范 `btn_{color}_{state}.png`。

---

### 展示C ← 你立刻要做的事

**打开 `题库模板.md`**，照着格式填题。

目标：200 条，文件 `questions-fallback.json`（已在桌面文件夹里）。

每条题目的格式：
```json
{
  "type": "color",
  "difficulty": 1,
  "instruction_text": "点蓝色的",
  "correct_action": "tap_blue",
  "options": [
    {"label": "红", "action": "tap_red", "color": "#EE4444"},
    {"label": "蓝", "action": "tap_blue", "color": "#4444DD"}
  ],
  "time_limit_ms": 1000,
  "tip": "蓝色和红色容易混淆，测试注意力"
}
```

支持的题型：`color` / `direction` / `math` / `double_neg` / `combo`

---

### 后端/架构 ← 现在做的

1. **保持后端运行**（已完成）
2. **盯前端对接**：前端A 调不通接口立刻排查
3. **准备 Demo 材料**：第 22 小时后帮展示C 准备演示流程

---

## 3. 关键 Checkpoint

| 时间 | 谁 | 必须产出 |
|------|-----|---------|
| 第 1h | 全队 | 所有人回复"收到了，开工" |
| 第 4h | 前端A | `index.html` 可玩（哪怕 1 种题型） |
| 第 6h | 策划C | 题库 ≥ 100 条 |
| 第 8h | UI·B | `design-tokens.json` + 按钮切图 |
| 第 22h | 全队 | 完整演示排练 |

---

## 4. 发群里的话（直接复制）

```
📣 后端已跑通，DeepSeek AI 出题已接通。

各人现在要做的事：
1. 前端A：读 api-contract.json，用 api-client.js 对接后端，4h 内出可玩版本
2. UI·B：出 design-tokens.json 和按钮切图
3. 策划C：按题库模板.md 填题，目标200条

文件都在 桌面/抖音创变者计划/ 里。

有问题直接在群里沟通。
```
